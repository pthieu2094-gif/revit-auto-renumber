<Window x:Class="SheetNumberingRevit.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SheetNumberingRevit.Commands.ViewModels"
        xmlns:helpers="clr-namespace:SheetNumberingRevit.Commands.Helpers"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="Đánh Số Sheet - Sheet Numbering"
        Height="660" Width="880"
        MinHeight="520" MinWidth="720"
        WindowStartupLocation="CenterScreen"
        Background="#ECE9E9"
        FontFamily="Segoe UI"
        FontSize="12"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel, IsDesignTimeCreatable=True}">

        <!-- ========================================================================
         RESOURCES
         ======================================================================== -->
    <Window.Resources>
        <helpers:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <helpers:InverseBoolConverter x:Key="InverseBoolConverter" />
        <helpers:CountToVisibilityConverter x:Key="CountToVisibilityConverter" />

        <!-- Primary button (blue) -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#1B3A6B" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="14,6" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="#0D2240" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2A5298" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0D2240" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#B0B0B0" />
                                <Setter Property="Foreground" Value="#808080" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary button (white / outline) -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="Foreground" Value="#1B3A6B" />
            <Setter Property="Padding" Value="14,6" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="#A0A0A0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontWeight" Value="Normal" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#EEF2F8" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#1B3A6B" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#DDE4EF" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#F0F0F0" />
                                <Setter Property="Foreground" Value="#909090" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Small icon button (arrow up/down) -->
        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#555555" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Width" Value="24" />
            <Setter Property="Height" Value="24" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="2"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#D8D8D8" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#BFBFBF" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#C0C0C0" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="5,4" />
            <Setter Property="BorderBrush" Value="#A0A0A0" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="White" />
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#1B3A6B" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- CheckBox -->
        <Style TargetType="CheckBox">
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>

        <!-- DataGrid column header -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#D6DCE4" />
            <Setter Property="Foreground" Value="#1B1B1B" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="BorderBrush" Value="#B0B8C4" />
            <Setter Property="BorderThickness" Value="0,0,1,1" />
        </Style>

        <!-- DataGrid cell -->
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="6,4" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#BDD7EE" />
                    <Setter Property="Foreground" Value="#1B1B1B" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- DataGrid row -->
        <Style TargetType="DataGridRow">
            <Setter Property="BorderBrush" Value="#D8D8D8" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#BDD7EE" />
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E8EFF7" />
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <!-- ========================================================================
         MAIN LAYOUT
         ======================================================================== -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#1B3A6B" Padding="14,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="ĐÁNH SỐ SHEET" FontSize="15" FontWeight="Bold" Foreground="White" />
                    <TextBlock Text="Tự động đánh số Sheet Number cho các bản vẽ trong Revit"
                               FontSize="11" Foreground="#B8C8DC" Margin="0,2,0,0" />
                </StackPanel>

                <Border Grid.Column="1" Background="#2A5298" CornerRadius="3" Padding="12,6" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Đã chọn: " Foreground="White" VerticalAlignment="Center" FontSize="12" />
                        <TextBlock Text="{Binding SelectedCount}" FontWeight="Bold" FontSize="14" Foreground="White" VerticalAlignment="Center" />
                        <TextBlock Text=" / " Foreground="#B8C8DC" VerticalAlignment="Center" />
                        <TextBlock Text="{Binding TotalCount}" Foreground="#B8C8DC" VerticalAlignment="Center" FontSize="12" />
                        <TextBlock Text=" sheet" Foreground="#B8C8DC" VerticalAlignment="Center" FontSize="12" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="12,12,12,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="230" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- LEFT: Options -->
            <Border Grid.Column="0" Background="White" BorderBrush="#B0B8C4" BorderThickness="1" Padding="14,12">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="TÙY CHỌN ĐÁNH SỐ" FontWeight="Bold" FontSize="11" Foreground="#555555" Margin="0,0,0,12" />

                        <TextBlock Text="Prefix (Tiền tố)" FontWeight="SemiBold" Foreground="#1B1B1B" Margin="0,0,0,6" />
                        <CheckBox Content="Sử dụng tiền tố" IsChecked="{Binding UsePrefix, Mode=TwoWay}" ToolTip="Bỏ chọn để đánh số không có tiền tố." Margin="0,0,0,5" />
                        <TextBox Text="{Binding Prefix, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding UsePrefix}" MaxLength="20" Margin="0,0,0,3" />
                        <TextBlock Text="Ví dụ: A-1, A-01, A-001" Foreground="#808080" FontStyle="Italic" FontSize="10" Margin="0,0,0,14" />

                        <TextBlock Text="Số bắt đầu (Start Number)" FontWeight="SemiBold" Foreground="#1B1B1B" Margin="0,0,0,6" />
                        <TextBox Text="{Binding StartNumber, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,14" />

                        <TextBlock Text="Số chữ số (Padding)" FontWeight="SemiBold" Foreground="#1B1B1B" Margin="0,0,0,6" />
                        <CheckBox Content="Sử dụng số chữ số cố định" IsChecked="{Binding UseManualPadding, Mode=TwoWay}" ToolTip="Bỏ chọn để hệ thống tự tính số chữ số theo số sheet được chọn." Margin="0,0,0,5" />
                        <ComboBox SelectedValue="{Binding NumberPadding}" SelectedValuePath="Tag" IsEnabled="{Binding UseManualPadding}" Margin="0,0,0,4">
                            <ComboBoxItem Content="0" Tag="0" />
                            <ComboBoxItem Content="1" Tag="1" />
                            <ComboBoxItem Content="2" Tag="2" IsSelected="True" />
                            <ComboBoxItem Content="3" Tag="3" />
                            <ComboBoxItem Content="4" Tag="4" />
                        </ComboBox>

                        <TextBlock Margin="0,4,0,14" FontStyle="Italic" FontSize="10" Foreground="#808080">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="Tự động theo số sheet được chọn: " />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding UseManualPadding}" Value="True">
                                            <Setter Property="Text" Value="" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                            <Run Text="Tự động: " />
                            <Run Text="{Binding AutoDetectedPadding, Mode=OneWay}" />
                            <Run Text=" chữ số." />
                        </TextBlock>

                        <TextBlock Text="CHỌN SHEET" FontWeight="Bold" FontSize="11" Foreground="#555555" Margin="0,0,0,10" />
                        <StackPanel Orientation="Horizontal">
                            <Button Content="Chọn tất cả" Style="{StaticResource SecondaryButton}" Command="{Binding SelectAllCommand}" Padding="10,5" Margin="0,0,6,0" FontSize="11" />
                            <Button Content="Bỏ chọn tất cả" Style="{StaticResource SecondaryButton}" Command="{Binding DeselectAllCommand}" Padding="10,5" FontSize="11" />
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- RIGHT: Sheet List -->
            <Border Grid.Column="2" Background="White" BorderBrush="#B0B8C4" BorderThickness="1" Padding="10,10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="DANH SÁCH SHEET" FontWeight="Bold" FontSize="11" Foreground="#555555" Margin="0,0,0,8" />

                    <DataGrid Grid.Row="1"
                              x:Name="SheetsGrid"
                              ItemsSource="{Binding Sheets}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False" CanUserDeleteRows="False" CanUserReorderColumns="False"
                              SelectionMode="Extended" SelectionUnit="FullRow"
                              GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#D8D8D8"
                              BorderThickness="1" BorderBrush="#B0B8C4"
                              Background="White" RowBackground="White" AlternatingRowBackground="#F6F7F9"
                              HeadersVisibility="Column" RowHeight="30"
                              VerticalScrollBarVisibility="Auto">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Width="38" CanUserResize="False">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                  Checked="SheetCheckBox_Changed" Unchecked="SheetCheckBox_Changed"
                                                  HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="CurrentSheetNumber" Binding="{Binding CurrentSheetNumber}" Width="155" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Padding" Value="8,4" />
                                        <Setter Property="VerticalAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Sheet Name" Binding="{Binding SheetName}" Width="*" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Padding" Value="8,4" />
                                        <Setter Property="VerticalAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTemplateColumn Header="NewSheetNumber" Width="145">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding NewSheetNumber}" FontWeight="SemiBold" Foreground="#1B3A6B" Padding="8,4" VerticalAlignment="Center" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="" Width="72">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <Button Style="{StaticResource IconButton}"
                                                    Command="{Binding DataContext.MoveUpCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}" ToolTip="Lên" Width="26" Height="26">
                                                <TextBlock Text="▲" FontSize="9" />
                                            </Button>
                                            <Button Style="{StaticResource IconButton}"
                                                    Command="{Binding DataContext.MoveDownCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}" ToolTip="Xuống" Width="26" Height="26">
                                                <TextBlock Text="▼" FontSize="9" />
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>

        <!-- Error bar -->
        <Border Grid.Row="2" Background="#FFF3E0" BorderBrush="#FFB74D" BorderThickness="1" Padding="10,7" Margin="12,8,12,0"
                Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#BF360C" TextWrapping="Wrap" FontSize="12" />
        </Border>

        <!-- Footer -->
        <Border Grid.Row="3" Background="#F0F2F5" BorderBrush="#B0B8C4" BorderThickness="0,1,0,0" Padding="14,10" Margin="0,8,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" VerticalAlignment="Center" Foreground="#555555" FontSize="12" Margin="0,0,20,0" />

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="Apply" Style="{StaticResource SecondaryButton}" Command="{Binding PreviewCommand}" Margin="0,0,8,0" MinWidth="90" FontSize="12" />
                    <Button Content="OK" Style="{StaticResource PrimaryButton}" Command="{Binding ApplyAndCloseCommand}" Margin="0,0,8,0" MinWidth="90" FontSize="12" />
                    <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Command="{Binding CancelCommand}" MinWidth="70" FontSize="12" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
